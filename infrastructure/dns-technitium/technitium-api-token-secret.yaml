apiVersion: v1
kind: Secret
metadata:
  name: technitium-api-token
  namespace: default
type: Opaque
# The actual secret data should be created using kubectl with the real value:
# kubectl create secret generic technitium-api-token \
#   --namespace default \
#   --from-literal=token='your-technitium-api-token-here' \
#   --dry-run=client -o yaml | kubectl apply -f -
#
# NOTE: this file is a reference copy only, documenting the expected shape —
# it is not wired into any kustomization.yaml resources: list, so ArgoCD
# never applies it (this whole directory is a reference copy anyway —
# Technitium itself runs outside the cluster, see this directory's
# README.md). Belongs to the "external-dns" Technitium user (Administrators
# group) described there. The real Secret is created directly on the
# cluster via kubectl.
