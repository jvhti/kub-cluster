# Single replica is enough; it aggregates kubelet summaries, no writes.
replicas: 1
args:
  - --kubelet-preferred-address-types=InternalIP
resources:
  requests:
    cpu: 20m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 256Mi