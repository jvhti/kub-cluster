apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
helmCharts:
  - name: cloudnative-pg
    repo: https://cloudnative-pg.github.io/charts
    version: 0.29.0 # renovate: github-releases=cloudnative-pg/charts&extractVersion=^cloudnative-pg-v(?<version>.*)$
    releaseName: cnpg
    namespace: cnpg
    includeCRDs: true
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-10"
